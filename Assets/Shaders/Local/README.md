# Your shaders

Drop `.shader` and `.shadergraph` files here, then open **Shader Gallery → Shader List**,
hit Rescan, tick them, hit Apply.

Nothing writes to this folder: the sync workflow only replaces `Assets/Shaders/Shared/`,
so pulling updates can't touch your work.
